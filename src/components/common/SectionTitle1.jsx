

const SectionTitle1 = ({ sub, title }) => {
    return (
        <>
            <span className="text-[#6278FF] font-bold">{sub}</span>
            <h3 className="text-2xl font-extrabold text-gray-800 w-60 mb-8">{title}</h3>
        </>
    );
};

export default SectionTitle1;